
Entrada=input('Introduce un numero para el triangulo: ');
S = '*';
T = Entrada;
for i = 0:Entrada
 K = ((T*2)-1);
 T = T -1;
 SC = '*';
    for j = 1:K
        SC = strcat(SC,S);
    end 
disp(SC)
end 
